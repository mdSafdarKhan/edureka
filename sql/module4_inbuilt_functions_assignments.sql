#Determine the Count of rows satisfying certain condition
select count(employee.hire_date) from employee;

#Determine average of a column
select avg(employee.mgr_id) from employee;

#Determine Sum of a column
select sum(employee.mgr_id) from employee;